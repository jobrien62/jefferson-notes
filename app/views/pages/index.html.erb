<h1>Listing pages</h1>
<pre>
Page:
</pre>
<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Page number</th>
      <th>Notes</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= page.content %></td>
        <td><%= page.page_number %></td>
        <td><%= page.notes %></td>
        <td><%= link_to 'Show', page %></td>
        <td><%= link_to 'Edit', edit_witness_page_path(page) %></td>
        <td><%= link_to 'Destroy', page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Page', new_witness_page_path %>
